# Curated allowlist of complement CS-API tests run against neutrino.
#
# Each non-comment line becomes its own `go test -run <line>` invocation
# (see scripts/complement.sh). One entry per line. Use the full slash-
# separated test path; trailing `$` anchors the match where a name is a
# prefix of a sibling.

# Versions
TestVersionStructure

# Login: subtests that work against the fixed-credential /login stub.
TestLogin/parallel/GET_/login_yields_a_set_of_flows$
TestLogin/parallel/POST_/login_can_login_as_user$
TestLogin/parallel/POST_/login_can_log_in_as_a_user_with_just_the_local_part_of_the_id$

# Registration: subtests that work against the single-user /register stub.
TestRegistration/parallel/POST_\{\}_returns_a_set_of_flows$
TestRegistration/parallel/POST_/register_can_create_a_user$
TestRegistration/parallel/POST_/register_returns_the_same_device_id_as_that_in_the_request$
TestRegistration/parallel/POST_/register_allows_registration_of_usernames_with_$
TestRegistration/parallel/Registration_accepts_non-ascii_passwords$

# Room creation: basic public/private room flows.
TestRoomCreate/Parallel/POST_/createRoom_makes_a_private_room$
TestRoomCreate/Parallel/POST_/createRoom_makes_a_private_room_with_invites$
TestRoomCreate/Parallel/POST_/createRoom_makes_a_public_room$

# Round-trip via /v3/sync: created room's m.room.create / m.room.member /
# subsequently-PUT message events surface in alice's own sync. Single user,
# no /join or /state endpoints exercised — pure validation of the legacy
# sync stub over create_room + put_event.
TestRoomCreate/Parallel/Can_/sync_newly_created_room$
TestRoomCreationReportsEventsToMyself/parallel/Room_creation_reports_m.room.create_to_myself$
TestRoomCreationReportsEventsToMyself/parallel/Room_creation_reports_m.room.member_to_myself$

# Membership/sync flows unlocked by the global POST /v3/join/{roomIdOrAlias}
# endpoint (2026-06-02) + the multi-user shim, confirmed against a Complement
# run. Four server fixes came out of that run: empty-body extractor, kick
# non-member 403, trailing-slash empty state key, and legacy /sync recovering a
# stale since token via a full re-sync instead of M_UNKNOWN_POS (the last
# un-parks TestCumulativeJoinLeaveJoinSync). One test dropped as not viable, see
# VIABLE-TESTS.md: TestMembersLocal (its fixture sets presence, an out-of-scope
# EDU). Trailing `$` anchors each name.
TestCumulativeJoinLeaveJoinSync$
TestGetRoomMembers$
TestRoomMembers/Parallel/POST_/rooms/:room_id/join_can_join_a_room$
TestRoomMembers/Parallel/POST_/join/:room_id_can_join_a_room$
TestRoomsInvite/Parallel/Can_invite_users_to_invite-only_rooms$
TestRoomsInvite/Parallel/Uninvited_users_cannot_join_the_room$
TestRoomsInvite/Parallel/Invited_user_can_reject_invite$
TestRoomsInvite/Parallel/Invited_user_can_reject_invite_for_empty_room$
TestRoomsInvite/Parallel/Users_cannot_invite_themselves_to_a_room$
TestRoomsInvite/Parallel/Users_cannot_invite_a_user_that_is_already_in_the_room$
TestCannotKickNonPresentUser$
TestCannotKickLeftUser$
TestNotPresentUserCannotBanOthers$

# Unlocked by GET room state (2026-06-03): read-back of current state via
# GET /rooms/{room}/state[/{type}[/{key}]]. Covers the TestRoomState GET-state
# subtests only; NOT joined_rooms / joined_members / publicRooms / directory
# (separate unimplemented endpoints). Regex notes (Go splits -run on `/` and
# matches each segment UNANCHORED, so a short segment substring-matches and
# silently drags in siblings): the `/^rooms$/` segment is anchored so it can't
# match the sibling `joined_rooms` subtest. `\?` escapes the literal `?` in the
# format=event name; trailing `$` anchors prefix-siblings (e.g. "…with_a_topic"
# vs "…with_a_topic_and_writes_rich…"). TODO: anchor segments in the allowlist
# parser (scripts/complement.sh) so per-entry anchoring isn't needed.
TestRoomState/Parallel/GET_/^rooms$/:room_id/state/m.room.member/:user_id_fetches_my_membership$
TestRoomState/Parallel/GET_/^rooms$/:room_id/state/m.room.member/:user_id\?format=event_fetches_my_membership_event$
TestRoomState/Parallel/GET_/^rooms$/:room_id/state/m.room.power_levels_fetches_powerlevels$
TestRoomState/Parallel/GET_/^rooms$/:room_id/state/m.room.name_gets_name$
TestRoomState/Parallel/POST_/^rooms$/:room_id/state/m.room.name_sets_name$
TestRoomState/Parallel/GET_/^rooms$/:room_id/state/m.room.topic_gets_topic$
TestRoomState/Parallel/PUT_/^rooms$/:room_id/state/m.room.topic_sets_topic$
TestRoomState/Parallel/GET_/^rooms$/:room_id/state_fetches_entire_room_state$
TestRoomMembers/Parallel/POST_/rooms/:room_id/ban_can_ban_a_user$
TestRoomMembers/Parallel/POST_/rooms/:room_id/invite_can_send_an_invite$
TestRoomMembers/Parallel/POST_/rooms/:room_id/leave_can_leave_a_room$
TestRoomCreate/Parallel/POST_/createRoom_makes_a_room_with_a_topic$
TestRoomCreate/Parallel/POST_/createRoom_makes_a_room_with_a_name$

# Viable 2026-06-03 (no new server code). These exercise only already-wired
# capability: createRoom name/topic/invite + PUT/GET state + membership
# (join/leave/invite) + bare legacy /sync with NO filter (a bare MustSyncUntil
# never touches the unimplemented filter API — sync.go:173).
#
# Two candidates were dropped after a CI run proved them incompatible with this
# server — real v12/design mismatches, NOT test-selection bugs:
#   - Test_that_we_can_be_reinvited_to_a_room_we_created: PUTs an m.room.power_levels
#     naming the creator (alice) in `users`, which room v12 auth rule 10.4 forbids
#     (creators hold implicit infinite PL and must not be listed) → 403 reject.
#   - Setting_state_twice_is_idempotent: asserts a repeated identical state PUT
#     returns the SAME event id; neutrino builds a fresh event per PUT (no
#     Synapse-style unchanged-state dedup, room_actor.rs:64-66), so ids differ.
# Also NOT included: TestPowerLevels "should not explode" (expects 403-not-500 on
# an empty-users PUT — needs an empirical run to confirm the reject is clean).
# Trailing `$` anchors prefix-siblings; `\(` `\)` escape the parens in (SYN-205).
TestRoomCreationReportsEventsToMyself/parallel/Setting_room_topic_reports_m.room.topic_to_myself$
TestRoomCreationReportsEventsToMyself/parallel/Joining_room_twice_is_idempotent$
TestTentativeEventualJoiningAfterRejecting$
TestRoomCreate/Parallel/Rooms_can_be_created_with_an_initial_invite_list_\(SYN-205\)$

# GET /rooms/{roomId}/messages (2026-06-04). The endpoint is join-gated, so a
# request for a room the user isn't in returns 403 — exactly what this test
# asserts. The other room_messages_test.go cases are not viable here (filters /
# lazy-loaded state, /send txn-id dedup, federation backfill) and
# TestSendAndFetchMessage is blocked on a durable stream-position legacy /sync
# token — see VIABLE-TESTS.md.
TestFetchMessagesFromNonExistentRoom$

# The E2EE and ephemeral surface (2026-09-03). Unlocked by the key transport
# and ephemeral-data work on this fork: real device/one-time key storage with
# `/keys/claim`, `/sendToDevice` delivered under `to_device`, `/typing` and
# `/receipt` surfaced through the sliding-sync extensions and folded into
# legacy `/sync`'s `ephemeral.events`, `/redact` applied on read, and
# `GET /rooms/{room}/event/{id}`. `TestUploadKey` runs whole: its query and
# claim subtests depend on the sibling upload having run first, and the two
# rejection subtests (malformed keys, another user's keys) pass here because
# `/keys/upload` validates. Not viable, and why: `TestKeyChangesLocal` and
# `TestDeviceListUpdates` need `device_lists.changed` (no
# `m.device_list_update` yet); `TestDeviceManagement` needs `/devices`;
# `TestRoomReadMarkers` needs room account data.
TestTyping$
TestLeakyTyping$
TestRoomReceipts$
TestRedact$
TestToDeviceMessages$
TestUploadKey$
TestUploadKeyIdempotency$
TestUploadKeyIdempotencyOverlap$
TestKeyClaimOrdering$
TestKeysQueryWithDeviceIDAsObjectFails$

# Device-list updates (2026-09-03): a device change is announced to peers as
# `m.device_list_update` and to clients under `device_lists.changed`, and a
# login that names no device gets a fresh one under the shim. Not viable:
# `TestDeviceListUpdates` — its local subtests expect `device_lists.changed`
# and `.left` to follow room membership (a user newly sharing, or no longer
# sharing, an encrypted room), which this server does not derive; clients
# built on matrix-sdk-crypto track room members themselves, so nothing we
# ship depends on it.
TestKeyChangesLocal$

# Identity and account data (2026-09-03): `/account/whoami`, global and
# per-room account data persisted and served by sync, a to-device inbox
# keyed per device. `TestRoomReadMarkers` stays out: `/read_markers` is not
# wired.
TestAddAccountData$

# Media (2026-09-03): upload, authenticated and legacy download, config with
# the cap, federation download. `TestRoomImageRoundtrip` stays out: it reads
# the room back through a `/messages` filter (`contains_url`), which is not
# wired. `TestAsyncUpload` stays out: no `/create` for async uploads.
TestMediaConfig$
